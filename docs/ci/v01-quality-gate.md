# V0.1 Quality Gate

Tệp này chỉ dùng để kích hoạt GitHub Actions kiểm tra `npm ci`, `npm run lint`, `npm run build` trên trạng thái `main` hiện tại của TUAN OS Enterprise.

Không thay đổi runtime, database, cấu hình production hoặc logic CMI/AI Marketing.
